{% extends 'base.html.twig' %}

{% block title %}RobotHumain{% endblock %}

{% block body %}
    <h1>RobotHumain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ robot_humain.id }}</td>
            </tr>
            <tr>
                <th>SerialNumber</th>
                <td>{{ robot_humain.SerialNumber }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ robot_humain.Name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ robot_humain.Surname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('robot_humain_index') }}">back to list</a>

    <a href="{{ path('robot_humain_edit', {'id': robot_humain.id}) }}">edit</a>

    {{ include('robot_humain/_delete_form.html.twig') }}
{% endblock %}
